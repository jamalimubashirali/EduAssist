# Onboarding (Minimal Pages)

Pages live here and avoid shared GameLayout while onboarding:
- /onboarding/landing
- /onboarding/signup
- /onboarding/avatar
- /onboarding/diagnostic

Flow: landing → signup → avatar → diagnostic → generate quiz → take quiz → results → dashboard

Skippable at any step.

Ensure NEXT_PUBLIC_USE_REAL_API=true for real data.
